#include <iostream>
using namespace std;

int main(int argn, char** argv){
	
	char c;
	
	cin >> c;
	
	if(c >= '0' && c <= '9') cout << "cifra decimale";
	if(c >= 'a' && c <) 'z') cout << "lettera minuscola";
	if(c >= 'A' && c <) 'Z') cout << "lettera maiuscola";
	
	return 0;
}
